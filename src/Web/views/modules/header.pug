nav(class="navbar navbar-expand-lg navbar-light homer-navbar")
  a(class="navbar-brand" href="/")
    img(class="d-inline-block align-top" src="/images/homer-icon.png" height="30")
    span(class="homer-navbrand") Homer

  button(class="navbar-toggler" type="button" data-toggle="collapse" data-target="#homer-navbar" aria-controls="homer-navbar" aria-expanded="false")
    span(class="navbar-toggler-icon")

  div(class="collapse navbar-collapse" id="homer-navbar")
    ul(class="navbar-nav mr-auto")
      li(class="nav-item")
        a(class="nav-link homer-navlink" href="/") #{__('dashboard.header.home')}
      li(class="nav-item")
        a(class="nav-link homer-navlink" href="/features") #{__('dashboard.header.features')}
      li(class="nav-item")
        a(class="nav-link homer-navlink" href="/support") #{__('dashboard.header.support')}

    ul(class="navbar-nav navbar-right")
      div(class="row")
        div(class="col-sm")
          a(class="btn btn-dark" href="/invite" role="button")
            img(class="d-inline-block align-top" src="/images/discord-white-logo.png" height="30")
            #{__('dashboard.header.inviteDiscord')}
        div(class="col-sm")
          div(class="dropdown dropleft")
            button(class="btn btn-secondary btn-transparent dropdown-toggle" type="button" id="languageDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false")
              img(src="/images/icons/translate-icon.svg" height="20")
            div(class="dropdown-menu" aria-labelledby="languageDropdown")
              each locale in locales
                a(class="dropdown-item" href=`/locale/${locale.code}`) #{locale.name}