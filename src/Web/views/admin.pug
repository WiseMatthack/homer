//- Admin - Represents the admin section.

<!DOCTYPE html>
html(lang=`${locale}`)
  head
    include modules/head.pug
  body
    include modules/header.pug

    div(class="jumbotron jumbotron-fluid")
      div(class="container text-center")
        h1(class="mbr-section-title mbr-bold mbr-fonts-style text-center") 📰 Manage articles
        hr
        form(action="/admin/sendArticle" method="POST")
          p
            input(type="text" id="title" name="title" placeholder="Title of the article")
          p
            textarea(id="content" name="content" rows="5")

          input(type="submit" value="Send article")

    include modules/footer.pug

    include modules/javascript.pug