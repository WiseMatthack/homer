<!DOCTYPE html>
html(lang=`${locale}`)
  head
    include modules/head.pug
  body
    include modules/header.pug

    div(class="jumbotron jumbotron-fluid")
      div(class="container text-center")
        h1(class="mbr-section-title mbr-bold mbr-fonts-style text-center") Maintenance
        hr
        form(action="/admin/maintenance" method="POST")
          label(for="maintenanceReason") Reason
          input(type="text" id="maintenanceReason" name="reason" placeholder="No reason specified")

          button(type="submit") Enable maintenance mode
        
    include modules/footer.pug

    include modules/javascript.pug